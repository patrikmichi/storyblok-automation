# Workspace-level ignores
.DS_Store
Thumbs.db
*.log

# Dependencies
node_modules/
package-lock.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Cursor IDE (user-specific, not committed)
.cursor/
# Template rules are in cursor-rules-template/ folder

# Environment variables and secrets
.env
.env.*
!.env.example
*.env
*.env.local
*.env.development.local
*.env.test.local
*.env.production.local

# Secrets and credentials
secrets/
*.secret
*.key
*.pem
*.p12
*.pfx
credentials.json
config.json
.secrets

# Webhook secrets and URLs
*webhook*.secret
*webhook*.key
webhook-config.json

# API keys and tokens
*api*key*
*token*
*password*
*auth*

# Each repository has its own .gitignore
# See schema-generator/.gitignore and storyblok-app/.gitignore
